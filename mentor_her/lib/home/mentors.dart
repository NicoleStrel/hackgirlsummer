import 'package:flutter/material.dart';

class Mentors extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('The mentor information goes here'),
    );
  }
}